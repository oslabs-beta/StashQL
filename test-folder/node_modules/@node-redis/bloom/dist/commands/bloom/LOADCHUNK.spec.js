"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const assert_1 = require("assert");
const test_utils_1 = require("../../test-utils");
const LOADCHUNK_1 = require("./LOADCHUNK");
describe('BF LOADCHUNK', () => {
    it('transformArguments', () => {
        assert_1.strict.deepEqual((0, LOADCHUNK_1.transformArguments)('key', 0, ''), ['BF.LOADCHUNK', 'key', '0', '']);
    });
    test_utils_1.default.testWithClient('client.bf.loadChunk', async (client) => {
        const [, { iterator, chunk }] = await Promise.all([
            client.bf.reserve('source', 0.01, 100),
            client.bf.scanDump(client.commandOptions({ returnBuffers: true }), 'source', 0)
        ]);
        assert_1.strict.equal(await client.bf.loadChunk('destination', iterator, chunk), 'OK');
    }, test_utils_1.GLOBAL.SERVERS.OPEN);
});
