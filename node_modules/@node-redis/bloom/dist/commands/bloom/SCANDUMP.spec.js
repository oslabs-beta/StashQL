"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const assert_1 = require("assert");
const test_utils_1 = require("../../test-utils");
const SCANDUMP_1 = require("./SCANDUMP");
describe('BF SCANDUMP', () => {
    it('transformArguments', () => {
        assert_1.strict.deepEqual((0, SCANDUMP_1.transformArguments)('key', 0), ['BF.SCANDUMP', 'key', '0']);
    });
    test_utils_1.default.testWithClient('client.bf.scanDump', async (client) => {
        const [, dump] = await Promise.all([
            client.bf.reserve('key', 0.01, 100),
            client.bf.scanDump('key', 0)
        ]);
        assert_1.strict.equal(typeof dump, 'object');
        assert_1.strict.equal(typeof dump.iterator, 'number');
        assert_1.strict.equal(typeof dump.chunk, 'string');
    }, test_utils_1.GLOBAL.SERVERS.OPEN);
});
